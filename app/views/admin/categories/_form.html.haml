- url = @category.new_record? ? admin_categories_path : admin_category_path(@category)
= bootstrap_form_for(@category, url: url, inline_errors: true) do |f|
  = f.text_field :name
  .actions
    = f.submit 'Save', class: "btn btn-primary", data: {"disable-with": "Saving...."}
