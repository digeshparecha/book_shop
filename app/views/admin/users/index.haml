.d-flex.py-2
  .flex-grow-1
    %h1 Users
  
%table.table.table-striped
  %thead
    %tr
      %th Fist Name
      %th Last Name
      %th Email
      %th Admin

  %tbody
    - @users.each do |user|
      %tr
        %td= user.first_name
        %td= user.last_name
        %td= user.email
        %td= user.is_admin ? "Yes" : "No"